_schema-version: "3.2"
ID: chunigefacturationinterne
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: chunigefacturationinterne-destination-content
  type: com.sap.application.content
  requires:
  - name: chunigefacturationinterne-destination-service
    parameters:
      content-target: true
  - name: chunigefacturationinterne-repo-host
    parameters:
      service-key:
        name: chunigefacturationinterne-repo-host-key
  - name: chunigefacturationinterne-uaa
    parameters:
      service-key:
        name: chunigefacturationinterne-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: chunigefacturationinterne_html_repo_host
          ServiceInstanceName: chunigefacturationinterne-html5-srv
          ServiceKeyName: chunigefacturationinterne-repo-host-key
          sap.cloud.service: chunigefacturationinterne
        - Authentication: OAuth2UserTokenExchange
          Name: chunigefacturationinterne_uaa
          ServiceInstanceName: chunigefacturationinterne-xsuaa-srv
          ServiceKeyName: chunigefacturationinterne-uaa-key
          sap.cloud.service: chunigefacturationinterne
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: chunigefacturationinterne-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: chunigefacturationinterne-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - chunigefacturationinterne.zip
      name: chunigefacturationinterne
      target-path: resources/
- name: chunigefacturationinterne
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: chunigefacturationinterne-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: chunigefacturationinterne-destination-service
    service-plan: lite
- name: chunigefacturationinterne-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: chunigefacturationinterne-xsuaa-srv
    service-plan: application
- name: chunigefacturationinterne-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: chunigefacturationinterne-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
